  // Copyright (c) Obelisk Labs, Inc.
  // SPDX-License-Identifier: Apache-2.0
  #[allow(unused_use)]
  
  /* Autogenerated file. Do not edit manually. */
  
  module example::resource2 { 
    use sui::bcs::{to_bytes};
    use std::ascii::{string, String, into_bytes};
    use dubhe::table_id;
    use dubhe::dapp_service::{Self, DappHub};
    use dubhe::dapp_system;
    use example::dapp_key;
    use example::dapp_key::DappKey;
    use example::direction;
    use example::direction::{Direction};

    const TABLE_NAME: vector<u8> = b"resource2";
    const OFFCHAIN: bool = false;

    public struct Resource2 has copy, drop, store {
        player: address,
        value: u32,
        direction: Direction,
    }

    public fun new(player: address, value: u32, direction: Direction): Resource2 {
        Resource2 {
            player,
            value,
            direction,
        }
    }

    public fun player(self: &Resource2): address {
        self.player
    }

    public fun value(self: &Resource2): u32 {
        self.value
    }

    public fun direction(self: &Resource2): Direction {
        self.direction
    }

    public fun update_player(self: &mut Resource2, player: address) {
        self.player = player
    }

    public fun update_value(self: &mut Resource2, value: u32) {
        self.value = value
    }

    public fun update_direction(self: &mut Resource2, direction: Direction) {
        self.direction = direction
    }

    public fun has(dapp_hub: &DappHub, resource_account: String): bool {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::has_record<DappKey>(dapp_hub, resource_account, key_tuple)
    }

    public fun ensure_has(dapp_hub: &DappHub, resource_account: String) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_record<DappKey>(dapp_hub, resource_account, key_tuple)
    }

    public fun ensure_has_not(dapp_hub: &DappHub, resource_account: String) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_not_record<DappKey>(dapp_hub, resource_account, key_tuple)
    }
  

    public(package) fun delete(dapp_hub: &mut DappHub, resource_account: String) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::delete_record<DappKey>(dapp_hub, dapp_key::new(), key_tuple, resource_account);
    }

    public fun get_player(dapp_hub: &DappHub, resource_account: String): address {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = dapp_system::get_field<DappKey>(dapp_hub, resource_account, key_tuple, 0);
        let mut bsc_type = sui::bcs::new(value);
        let player = sui::bcs::peel_address(&mut bsc_type);
        player
    }

    public(package) fun set_player(dapp_hub: &mut DappHub, resource_account: String, player: address, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&player);
        dapp_system::set_field(dapp_hub, dapp_key::new(), resource_account, key_tuple, 0, value, ctx);
    }

    public fun get_value(dapp_hub: &DappHub, resource_account: String): u32 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = dapp_system::get_field<DappKey>(dapp_hub, resource_account, key_tuple, 1);
        let mut bsc_type = sui::bcs::new(value);
        let value = sui::bcs::peel_u32(&mut bsc_type);
        value
    }

    public(package) fun set_value(dapp_hub: &mut DappHub, resource_account: String, value: u32, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&value);
        dapp_system::set_field(dapp_hub, dapp_key::new(), resource_account, key_tuple, 1, value, ctx);
    }

    public fun get_direction(dapp_hub: &DappHub, resource_account: String): Direction {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = dapp_system::get_field<DappKey>(dapp_hub, resource_account, key_tuple, 2);
        let mut bsc_type = sui::bcs::new(value);
        let direction = example::direction::decode(&mut bsc_type);
        direction
    }

    public(package) fun set_direction(dapp_hub: &mut DappHub, resource_account: String, direction: Direction, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = example::direction::encode(direction);
        dapp_system::set_field(dapp_hub, dapp_key::new(), resource_account, key_tuple, 2, value, ctx);
    }

    public fun get(dapp_hub: &DappHub, resource_account: String): (address, u32, Direction) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value_tuple = dapp_system::get_record<DappKey>(dapp_hub, resource_account, key_tuple);
        let mut bsc_type = sui::bcs::new(value_tuple);
        let player = sui::bcs::peel_address(&mut bsc_type);
        let value = sui::bcs::peel_u32(&mut bsc_type);
        let direction = example::direction::decode(&mut bsc_type);
        (player, value, direction)
    }

    public(package) fun set(dapp_hub: &mut DappHub, resource_account: String, player: address, value: u32, direction: Direction, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value_tuple = encode(player, value, direction);
        dapp_system::set_record(dapp_hub, dapp_key::new(), key_tuple, value_tuple, resource_account, OFFCHAIN, ctx);
    }

    public fun get_struct(dapp_hub: &DappHub, resource_account: String): Resource2 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value_tuple = dapp_system::get_record<DappKey>(dapp_hub, resource_account, key_tuple);
        decode(value_tuple)
    }

    public(package) fun set_struct(dapp_hub: &mut DappHub, resource_account: String, resource2: Resource2, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value_tuple = encode_struct(resource2);
        dapp_system::set_record(dapp_hub, dapp_key::new(), key_tuple, value_tuple, resource_account, OFFCHAIN, ctx);
    }

    public fun encode(player: address, value: u32, direction: Direction): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&player));
        value_tuple.push_back(to_bytes(&value));
        value_tuple.push_back(example::direction::encode(direction));
        value_tuple
    }

    public fun encode_struct(resource2: Resource2): vector<vector<u8>> {
        encode(resource2.player, resource2.value, resource2.direction)
    }

    public fun decode(data: vector<u8>): Resource2 {
        let mut bsc_type = sui::bcs::new(data);
        let player = sui::bcs::peel_address(&mut bsc_type);
        let value = sui::bcs::peel_u32(&mut bsc_type);
        let direction = example::direction::decode(&mut bsc_type);
        Resource2 {
            player,
            value,
            direction,
        }
    }
}