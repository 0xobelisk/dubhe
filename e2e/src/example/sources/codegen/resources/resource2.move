// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0
#[allow(unused_use)]

/* Autogenerated file. Do not edit manually. */

module example::resource2 {
    use sui::bcs::{to_bytes};
    use std::ascii::{string, String, into_bytes};
    use dubhe::table_id;
    use dubhe::dapp_service::{Self, UserStorage, DappHub};
    use dubhe::dapp_system;
    use example::dapp_key;
    use example::dapp_key::DappKey;
    use example::direction;
    use example::direction::{Direction};

    const TABLE_NAME: vector<u8> = b"resource2";
    const OFFCHAIN: bool = false;

    public struct Resource2 has copy, drop, store {
        player: address,
        value: u32,
        direction: Direction,
    }

    public fun new(player: address, value: u32, direction: Direction): Resource2 {
        Resource2 {
            player,
            value,
            direction,
        }
    }

    public fun player(self: &Resource2): address {
        self.player
    }

    public fun value(self: &Resource2): u32 {
        self.value
    }

    public fun direction(self: &Resource2): Direction {
        self.direction
    }

    public fun update_player(self: &mut Resource2, player: address) {
        self.player = player
    }

    public fun update_value(self: &mut Resource2, value: u32) {
        self.value = value
    }

    public fun update_direction(self: &mut Resource2, direction: Direction) {
        self.direction = direction
    }

    public fun has(user_storage: &UserStorage): bool {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has_not(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_not_record<DappKey>(user_storage, key_tuple)
    }
  

    public(package) fun delete(user_storage: &mut UserStorage, ctx: &TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::delete_record<DappKey>(dapp_key::new(), user_storage, key_tuple, vector[b"player", b"value", b"direction"], ctx);
    }

    public fun get_player(user_storage: &UserStorage): address {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let player_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"player");
        let mut player_bcs = sui::bcs::new(player_raw);
        let player = sui::bcs::peel_address(&mut player_bcs);
        player
    }

    public(package) fun set_player(dapp_hub: &DappHub, user_storage: &mut UserStorage, player: address, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&player);
        dapp_system::set_field<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, b"player", value, ctx);
    }

    public fun get_value(user_storage: &UserStorage): u32 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"value");
        let mut value_bcs = sui::bcs::new(value_raw);
        let value = sui::bcs::peel_u32(&mut value_bcs);
        value
    }

    public(package) fun set_value(dapp_hub: &DappHub, user_storage: &mut UserStorage, value: u32, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&value);
        dapp_system::set_field<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, b"value", value, ctx);
    }

    public fun get_direction(user_storage: &UserStorage): Direction {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let direction_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"direction");
        let mut direction_bcs = sui::bcs::new(direction_raw);
        let direction = example::direction::decode(&mut direction_bcs);
        direction
    }

    public(package) fun set_direction(dapp_hub: &DappHub, user_storage: &mut UserStorage, direction: Direction, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = example::direction::encode(direction);
        dapp_system::set_field<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, b"direction", value, ctx);
    }

    public fun get(user_storage: &UserStorage): (address, u32, Direction) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let player_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"player");
        let mut player_bcs = sui::bcs::new(player_raw);
        let player = sui::bcs::peel_address(&mut player_bcs);
        let value_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"value");
        let mut value_bcs = sui::bcs::new(value_raw);
        let value = sui::bcs::peel_u32(&mut value_bcs);
        let direction_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"direction");
        let mut direction_bcs = sui::bcs::new(direction_raw);
        let direction = example::direction::decode(&mut direction_bcs);
        (player, value, direction)
    }

    public(package) fun set(dapp_hub: &DappHub, user_storage: &mut UserStorage, player: address, value: u32, direction: Direction, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"player", b"value", b"direction"];
        let value_tuple = encode(player, value, direction);
        dapp_system::set_record<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun get_struct(user_storage: &UserStorage): Resource2 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let player_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"player");
        let mut player_bcs = sui::bcs::new(player_raw);
        let player = sui::bcs::peel_address(&mut player_bcs);
        let value_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"value");
        let mut value_bcs = sui::bcs::new(value_raw);
        let value = sui::bcs::peel_u32(&mut value_bcs);
        let direction_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"direction");
        let mut direction_bcs = sui::bcs::new(direction_raw);
        let direction = example::direction::decode(&mut direction_bcs);
        Resource2 { player, value, direction }
    }

    public(package) fun set_struct(dapp_hub: &DappHub, user_storage: &mut UserStorage, resource2: Resource2, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"player", b"value", b"direction"];
        let value_tuple = encode_struct(resource2);
        dapp_system::set_record<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun encode(player: address, value: u32, direction: Direction): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&player));
        value_tuple.push_back(to_bytes(&value));
        value_tuple.push_back(example::direction::encode(direction));
        value_tuple
    }

    public fun encode_struct(resource2: Resource2): vector<vector<u8>> {
        encode(resource2.player, resource2.value, resource2.direction)
    }

    public fun decode(data: vector<u8>): Resource2 {
        let mut bcs_type = sui::bcs::new(data);
        let player = sui::bcs::peel_address(&mut bcs_type);
        let value = sui::bcs::peel_u32(&mut bcs_type);
        let direction = example::direction::decode(&mut bcs_type);
        Resource2 {
            player,
            value,
            direction,
        }
    }
}