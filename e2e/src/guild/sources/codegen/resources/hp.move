// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0
#[allow(unused_use)]

/* Autogenerated file. Do not edit manually. */

module guild::hp {
    use sui::bcs::{to_bytes};
    use std::ascii::{string, String, into_bytes};
    use dubhe::table_id;
    use dubhe::dapp_service::{Self, UserStorage};
    use dubhe::dapp_system;
    use guild::dapp_key;
    use guild::dapp_key::DappKey;


    const TABLE_NAME: vector<u8> = b"hp";
    const OFFCHAIN: bool = false;

    public struct Hp has copy, drop, store {
        current: u64,
        max: u64,
    }

    public fun new(current: u64, max: u64): Hp {
        Hp {
            current,
            max,
        }
    }

    public fun current(self: &Hp): u64 {
        self.current
    }

    public fun max(self: &Hp): u64 {
        self.max
    }

    public fun update_current(self: &mut Hp, current: u64) {
        self.current = current
    }

    public fun update_max(self: &mut Hp, max: u64) {
        self.max = max
    }

    public fun has(user_storage: &UserStorage): bool {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has_not(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_not_record<DappKey>(user_storage, key_tuple)
    }
  

    public(package) fun delete(user_storage: &mut UserStorage, ctx: &TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::delete_record<DappKey>(dapp_key::new(), user_storage, key_tuple, vector[b"current", b"max"], ctx);
    }

    public fun get_current(user_storage: &UserStorage): u64 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let current_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"current");
        let mut current_bcs = sui::bcs::new(current_raw);
        let current = sui::bcs::peel_u64(&mut current_bcs);
        current
    }

    public(package) fun set_current(user_storage: &mut UserStorage, current: u64, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&current);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"current", value, ctx);
    }

    public fun get_max(user_storage: &UserStorage): u64 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let max_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"max");
        let mut max_bcs = sui::bcs::new(max_raw);
        let max = sui::bcs::peel_u64(&mut max_bcs);
        max
    }

    public(package) fun set_max(user_storage: &mut UserStorage, max: u64, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&max);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"max", value, ctx);
    }

    public fun get(user_storage: &UserStorage): (u64, u64) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let current_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"current");
        let mut current_bcs = sui::bcs::new(current_raw);
        let current = sui::bcs::peel_u64(&mut current_bcs);
        let max_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"max");
        let mut max_bcs = sui::bcs::new(max_raw);
        let max = sui::bcs::peel_u64(&mut max_bcs);
        (current, max)
    }

    public(package) fun set(user_storage: &mut UserStorage, current: u64, max: u64, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"current", b"max"];
        let value_tuple = encode(current, max);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun get_struct(user_storage: &UserStorage): Hp {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let current_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"current");
        let mut current_bcs = sui::bcs::new(current_raw);
        let current = sui::bcs::peel_u64(&mut current_bcs);
        let max_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"max");
        let mut max_bcs = sui::bcs::new(max_raw);
        let max = sui::bcs::peel_u64(&mut max_bcs);
        Hp { current, max }
    }

    public(package) fun set_struct(user_storage: &mut UserStorage, hp: Hp, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"current", b"max"];
        let value_tuple = encode_struct(hp);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun encode(current: u64, max: u64): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&current));
        value_tuple.push_back(to_bytes(&max));
        value_tuple
    }

    public fun encode_struct(hp: Hp): vector<vector<u8>> {
        encode(hp.current, hp.max)
    }

    public fun decode(data: vector<u8>): Hp {
        let mut bcs_type = sui::bcs::new(data);
        let current = sui::bcs::peel_u64(&mut bcs_type);
        let max = sui::bcs::peel_u64(&mut bcs_type);
        Hp {
            current,
            max,
        }
    }


    // ─── reactive: cross-user write variants ───────────────────────────
    public(package) fun set_reactive(
        meta:   &dubhe::dapp_service::SceneMetadata,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        current: u64, max: u64,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"current", b"max"];
        let value_tuple = encode(current, max);
        dapp_system::set_record_reactive<DappKey>(dapp_key::new(), meta, from, target, key_tuple, field_names, value_tuple, ctx);
    }

    public(package) fun set_current_reactive(
        meta:   &dubhe::dapp_service::SceneMetadata,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        current: u64,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = sui::bcs::to_bytes(&current);
        dapp_system::set_field_reactive<DappKey>(dapp_key::new(), meta, from, target, key_tuple, b"current", value, ctx);
    }

    public(package) fun set_max_reactive(
        meta:   &dubhe::dapp_service::SceneMetadata,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        max: u64,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = sui::bcs::to_bytes(&max);
        dapp_system::set_field_reactive<DappKey>(dapp_key::new(), meta, from, target, key_tuple, b"max", value, ctx);
    }
}