// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

/* Autogenerated file. Do not edit manually. */

module guild::dungeon_run {
    use dubhe::dapp_service::DappStorage;
    use guild::dapp_key;
    use guild::dapp_key::DappKey;
    use guild::pvp_match;

    // ─── Error constants ───────────────────────────────────────────────────
    #[error]
    const EInsufficientAmount: vector<u8> = b"Insufficient amount";

    const SCENE_TYPE: vector<u8> = b"dungeon_run";

    /// Phantom type that distinguishes this scene storage at compile time.
    public struct DungeonRun has copy, drop {}

    // ─── Field accessors (own fields) ──────────────────────────────────────

    public fun get_floor(storage: &dubhe::dapp_service::SceneStorage<DungeonRun>): u32 {
        dubhe::dapp_system::get_scene_field<DungeonRun, u32>(storage, b"floor")
    }

    public(package) fun set_floor(
        permit:       &dubhe::dapp_service::ScenePermit<guild::dungeon_run_permit::DungeonRunPermit>,
        storage:      &mut dubhe::dapp_service::SceneStorage<DungeonRun>,
        user_storage: &dubhe::dapp_service::UserStorage,
        value:        u32,
        ctx:          &TxContext,
    ) {
        dubhe::dapp_system::set_scene_field<DappKey, guild::dungeon_run_permit::DungeonRunPermit, DungeonRun, u32>(
            dapp_key::new(), permit, storage, user_storage, b"floor", value, ctx
        );
    }

    public(package) fun remove_floor_system_maintenance(storage: &mut dubhe::dapp_service::SceneStorage<DungeonRun>): u32 {
        dubhe::dapp_system::remove_scene_field_system_maintenance<DappKey, DungeonRun, u32>(
            dapp_key::new(), storage, b"floor"
        )
    }

    public fun get_boss_id(storage: &dubhe::dapp_service::SceneStorage<DungeonRun>): u64 {
        dubhe::dapp_system::get_scene_field<DungeonRun, u64>(storage, b"boss_id")
    }

    public(package) fun set_boss_id(
        permit:       &dubhe::dapp_service::ScenePermit<guild::dungeon_run_permit::DungeonRunPermit>,
        storage:      &mut dubhe::dapp_service::SceneStorage<DungeonRun>,
        user_storage: &dubhe::dapp_service::UserStorage,
        value:        u64,
        ctx:          &TxContext,
    ) {
        dubhe::dapp_system::set_scene_field<DappKey, guild::dungeon_run_permit::DungeonRunPermit, DungeonRun, u64>(
            dapp_key::new(), permit, storage, user_storage, b"boss_id", value, ctx
        );
    }

    public(package) fun remove_boss_id_system_maintenance(storage: &mut dubhe::dapp_service::SceneStorage<DungeonRun>): u64 {
        dubhe::dapp_system::remove_scene_field_system_maintenance<DappKey, DungeonRun, u64>(
            dapp_key::new(), storage, b"boss_id"
        )
    }

    // ─── Bag accessors for accepted resources ─────────────────────────────

    public fun get_gold(storage: &dubhe::dapp_service::SceneStorage<DungeonRun>): u64 {
        if (dubhe::dapp_system::has_scene_field<DungeonRun, u64>(storage, b"gold")) {
            dubhe::dapp_system::get_scene_field<DungeonRun, u64>(storage, b"gold")
        } else { 0 }
    }

    public(package) fun add_gold(
        permit:  &dubhe::dapp_service::ScenePermit<guild::dungeon_run_permit::DungeonRunPermit>,
        storage: &mut dubhe::dapp_service::SceneStorage<DungeonRun>,
        user_storage: &dubhe::dapp_service::UserStorage,
        amount:  u64,
        ctx:     &TxContext,
    ) {
        let current = get_gold(storage);
        dubhe::dapp_system::set_scene_field<DappKey, guild::dungeon_run_permit::DungeonRunPermit, DungeonRun, u64>(
            dapp_key::new(), permit, storage, user_storage, b"gold", current + amount, ctx
        );
    }

    public(package) fun sub_gold(
        permit:  &dubhe::dapp_service::ScenePermit<guild::dungeon_run_permit::DungeonRunPermit>,
        storage: &mut dubhe::dapp_service::SceneStorage<DungeonRun>,
        user_storage: &dubhe::dapp_service::UserStorage,
        amount:  u64,
        ctx:     &TxContext,
    ) {
        let current = get_gold(storage);
        assert!(current >= amount, EInsufficientAmount);
        dubhe::dapp_system::set_scene_field<DappKey, guild::dungeon_run_permit::DungeonRunPermit, DungeonRun, u64>(
            dapp_key::new(), permit, storage, user_storage, b"gold", current - amount, ctx
        );
    }

    public fun get_loot(storage: &dubhe::dapp_service::SceneStorage<DungeonRun>): u64 {
        if (dubhe::dapp_system::has_scene_field<DungeonRun, u64>(storage, b"loot")) {
            dubhe::dapp_system::get_scene_field<DungeonRun, u64>(storage, b"loot")
        } else { 0 }
    }

    public(package) fun add_loot(
        permit:  &dubhe::dapp_service::ScenePermit<guild::dungeon_run_permit::DungeonRunPermit>,
        storage: &mut dubhe::dapp_service::SceneStorage<DungeonRun>,
        user_storage: &dubhe::dapp_service::UserStorage,
        amount:  u64,
        ctx:     &TxContext,
    ) {
        let current = get_loot(storage);
        dubhe::dapp_system::set_scene_field<DappKey, guild::dungeon_run_permit::DungeonRunPermit, DungeonRun, u64>(
            dapp_key::new(), permit, storage, user_storage, b"loot", current + amount, ctx
        );
    }

    public(package) fun sub_loot(
        permit:  &dubhe::dapp_service::ScenePermit<guild::dungeon_run_permit::DungeonRunPermit>,
        storage: &mut dubhe::dapp_service::SceneStorage<DungeonRun>,
        user_storage: &dubhe::dapp_service::UserStorage,
        amount:  u64,
        ctx:     &TxContext,
    ) {
        let current = get_loot(storage);
        assert!(current >= amount, EInsufficientAmount);
        dubhe::dapp_system::set_scene_field<DappKey, guild::dungeon_run_permit::DungeonRunPermit, DungeonRun, u64>(
            dapp_key::new(), permit, storage, user_storage, b"loot", current - amount, ctx
        );
    }

    // ─── acceptsFrom: cross-storage transfer functions ─────────────────────

    public(package) fun transfer_pvp_match_to_dungeon_run_loot(
        source_permit: &dubhe::dapp_service::ScenePermit<guild::pvp_match_permit::PvpMatchPermit>,
        dest_permit: &dubhe::dapp_service::ScenePermit<guild::dungeon_run_permit::DungeonRunPermit>,
        from:   &mut dubhe::dapp_service::SceneStorage<guild::pvp_match::PvpMatch>,
        to:     &mut dubhe::dapp_service::SceneStorage<DungeonRun>,
        user_storage: &dubhe::dapp_service::UserStorage,
        amount: u64,
        ctx:         &TxContext,
    ) {
        pvp_match::sub_loot(source_permit, from, user_storage, amount, ctx);
        add_loot(dest_permit, to, user_storage, amount, ctx);
    }

    // ─── SceneStorage lifecycle wrappers ──────────────────────────────────

    public(package) fun new_dungeon_run_with_permit(
        dapp_storage: &DappStorage,
        permit:       &dubhe::dapp_service::ScenePermit<guild::dungeon_run_permit::DungeonRunPermit>,
        ctx:          &mut TxContext,
    ): dubhe::dapp_service::SceneStorage<DungeonRun> {
        dubhe::dapp_system::new_typed_scene_with_permit<DappKey, guild::dungeon_run_permit::DungeonRunPermit, DungeonRun>(
            dapp_key::new(), dapp_storage, permit, SCENE_TYPE, ctx
        )
    }

    public(package) fun create_dungeon_run_with_permit(
        dapp_storage: &DappStorage,
        permit:       &dubhe::dapp_service::ScenePermit<guild::dungeon_run_permit::DungeonRunPermit>,
        ctx:          &mut TxContext,
    ) {
        dubhe::dapp_system::create_and_share_typed_scene_with_permit<DappKey, guild::dungeon_run_permit::DungeonRunPermit, DungeonRun>(
            dapp_key::new(), dapp_storage, permit, SCENE_TYPE, ctx
        );
    }

    public(package) fun share_dungeon_run(storage: dubhe::dapp_service::SceneStorage<DungeonRun>) {
        dubhe::dapp_system::share_scene_storage<DappKey, DungeonRun>(
            dapp_key::new(), storage
        );
    }

    public(package) fun destroy_dungeon_run(storage: dubhe::dapp_service::SceneStorage<DungeonRun>) {
        dubhe::dapp_system::destroy_typed_scene<DappKey, DungeonRun>(
            dapp_key::new(), storage
        );
    }
}
