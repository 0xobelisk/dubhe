apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dubhe-ingress
  namespace: dubhe
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/rate-limit-window: "1m"
spec:
  tls:
  - hosts:
    - api.dubhe.io
    - graphql.dubhe.io
    - docs.dubhe.io
    secretName: dubhe-tls
  rules:
  - host: api.dubhe.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dubhe-api-server
            port:
              number: 3000
  - host: graphql.dubhe.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dubhe-graphql-server
            port:
              number: 4000
  - host: docs.dubhe.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dubhe-docs
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dubhe-ingress-staging
  namespace: dubhe-staging
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
spec:
  tls:
  - hosts:
    - staging-api.dubhe.io
    - staging-graphql.dubhe.io
    secretName: dubhe-tls-staging
  rules:
  - host: staging-api.dubhe.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dubhe-api-server-staging
            port:
              number: 3000
  - host: staging-graphql.dubhe.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dubhe-graphql-server-staging
            port:
              number: 4000 