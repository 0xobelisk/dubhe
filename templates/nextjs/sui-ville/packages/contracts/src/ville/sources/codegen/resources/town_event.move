// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

/* Autogenerated file. Do not edit manually. */

module ville::town_event {
    use sui::bcs::{to_bytes};
    use dubhe::dapp_service::DappStorage;
    use dubhe::dapp_system;
    use ville::dapp_key;
    use ville::dapp_key::DappKey;


    const TABLE_NAME: vector<u8> = b"town_event";
    const OFFCHAIN: bool = false;

    public struct TownEvent has copy, drop, store {
        kind: u8,
        until: u64,
        magnitude: u64,
        started_day: u64,
    }

    public fun new(kind: u8, until: u64, magnitude: u64, started_day: u64): TownEvent {
        TownEvent {
            kind,
            until,
            magnitude,
            started_day,
        }
    }

    public fun kind(self: &TownEvent): u8 {
        self.kind
    }

    public fun until(self: &TownEvent): u64 {
        self.until
    }

    public fun magnitude(self: &TownEvent): u64 {
        self.magnitude
    }

    public fun started_day(self: &TownEvent): u64 {
        self.started_day
    }

    public fun update_kind(self: &mut TownEvent, kind: u8) {
        self.kind = kind
    }

    public fun update_until(self: &mut TownEvent, until: u64) {
        self.until = until
    }

    public fun update_magnitude(self: &mut TownEvent, magnitude: u64) {
        self.magnitude = magnitude
    }

    public fun update_started_day(self: &mut TownEvent, started_day: u64) {
        self.started_day = started_day
    }

    public fun has(dapp_storage: &DappStorage): bool {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::has_global_record<DappKey>(dapp_storage, key_tuple)
    }

    public fun ensure_has(dapp_storage: &DappStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_global_record<DappKey>(dapp_storage, key_tuple)
    }

    public fun ensure_has_not(dapp_storage: &DappStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_not_global_record<DappKey>(dapp_storage, key_tuple)
    }
  

    public(package) fun delete(dapp_storage: &mut DappStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::delete_global_record<DappKey>(dapp_key::new(), dapp_storage, key_tuple, vector[b"kind", b"until", b"magnitude", b"started_day"]);
    }

    public fun get_kind(dapp_storage: &DappStorage): u8 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let kind_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"kind");
        let mut kind_bcs = sui::bcs::new(kind_raw);
        let kind = sui::bcs::peel_u8(&mut kind_bcs);
        kind
    }

    public(package) fun set_kind(dapp_storage: &mut DappStorage, kind: u8) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&kind);
        dapp_system::set_global_field<DappKey>(dapp_key::new(), dapp_storage, key_tuple, b"kind", value);
    }

    public fun get_until(dapp_storage: &DappStorage): u64 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let until_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"until");
        let mut until_bcs = sui::bcs::new(until_raw);
        let until = sui::bcs::peel_u64(&mut until_bcs);
        until
    }

    public(package) fun set_until(dapp_storage: &mut DappStorage, until: u64) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&until);
        dapp_system::set_global_field<DappKey>(dapp_key::new(), dapp_storage, key_tuple, b"until", value);
    }

    public fun get_magnitude(dapp_storage: &DappStorage): u64 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let magnitude_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"magnitude");
        let mut magnitude_bcs = sui::bcs::new(magnitude_raw);
        let magnitude = sui::bcs::peel_u64(&mut magnitude_bcs);
        magnitude
    }

    public(package) fun set_magnitude(dapp_storage: &mut DappStorage, magnitude: u64) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&magnitude);
        dapp_system::set_global_field<DappKey>(dapp_key::new(), dapp_storage, key_tuple, b"magnitude", value);
    }

    public fun get_started_day(dapp_storage: &DappStorage): u64 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let started_day_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"started_day");
        let mut started_day_bcs = sui::bcs::new(started_day_raw);
        let started_day = sui::bcs::peel_u64(&mut started_day_bcs);
        started_day
    }

    public(package) fun set_started_day(dapp_storage: &mut DappStorage, started_day: u64) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&started_day);
        dapp_system::set_global_field<DappKey>(dapp_key::new(), dapp_storage, key_tuple, b"started_day", value);
    }

    public fun get(dapp_storage: &DappStorage): (u8, u64, u64, u64) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let kind_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"kind");
        let mut kind_bcs = sui::bcs::new(kind_raw);
        let kind = sui::bcs::peel_u8(&mut kind_bcs);
        let until_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"until");
        let mut until_bcs = sui::bcs::new(until_raw);
        let until = sui::bcs::peel_u64(&mut until_bcs);
        let magnitude_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"magnitude");
        let mut magnitude_bcs = sui::bcs::new(magnitude_raw);
        let magnitude = sui::bcs::peel_u64(&mut magnitude_bcs);
        let started_day_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"started_day");
        let mut started_day_bcs = sui::bcs::new(started_day_raw);
        let started_day = sui::bcs::peel_u64(&mut started_day_bcs);
        (kind, until, magnitude, started_day)
    }

    public(package) fun set(dapp_storage: &mut DappStorage, kind: u8, until: u64, magnitude: u64, started_day: u64) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"kind", b"until", b"magnitude", b"started_day"];
        let value_tuple = encode(kind, until, magnitude, started_day);
        dapp_system::set_global_record<DappKey>(dapp_key::new(), dapp_storage, key_tuple, field_names, value_tuple, OFFCHAIN);
    }

    public fun get_struct(dapp_storage: &DappStorage): TownEvent {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let kind_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"kind");
        let mut kind_bcs = sui::bcs::new(kind_raw);
        let kind = sui::bcs::peel_u8(&mut kind_bcs);
        let until_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"until");
        let mut until_bcs = sui::bcs::new(until_raw);
        let until = sui::bcs::peel_u64(&mut until_bcs);
        let magnitude_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"magnitude");
        let mut magnitude_bcs = sui::bcs::new(magnitude_raw);
        let magnitude = sui::bcs::peel_u64(&mut magnitude_bcs);
        let started_day_raw = dapp_system::get_global_field<DappKey>(dapp_storage, key_tuple, b"started_day");
        let mut started_day_bcs = sui::bcs::new(started_day_raw);
        let started_day = sui::bcs::peel_u64(&mut started_day_bcs);
        TownEvent { kind, until, magnitude, started_day }
    }

    public(package) fun set_struct(dapp_storage: &mut DappStorage, town_event: TownEvent) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"kind", b"until", b"magnitude", b"started_day"];
        let value_tuple = encode_struct(town_event);
        dapp_system::set_global_record<DappKey>(dapp_key::new(), dapp_storage, key_tuple, field_names, value_tuple, OFFCHAIN);
    }

    public fun encode(kind: u8, until: u64, magnitude: u64, started_day: u64): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&kind));
        value_tuple.push_back(to_bytes(&until));
        value_tuple.push_back(to_bytes(&magnitude));
        value_tuple.push_back(to_bytes(&started_day));
        value_tuple
    }

    public fun encode_struct(town_event: TownEvent): vector<vector<u8>> {
        encode(town_event.kind, town_event.until, town_event.magnitude, town_event.started_day)
    }

    public fun decode(data: vector<u8>): TownEvent {
        let mut bcs_type = sui::bcs::new(data);
        let kind = sui::bcs::peel_u8(&mut bcs_type);
        let until = sui::bcs::peel_u64(&mut bcs_type);
        let magnitude = sui::bcs::peel_u64(&mut bcs_type);
        let started_day = sui::bcs::peel_u64(&mut bcs_type);
        TownEvent {
            kind,
            until,
            magnitude,
            started_day,
        }
    }
}