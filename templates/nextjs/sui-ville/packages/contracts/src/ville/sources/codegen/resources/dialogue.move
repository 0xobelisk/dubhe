// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

/* Autogenerated file. Do not edit manually. */

module ville::dialogue {
    use sui::bcs::{to_bytes};
    use std::ascii::{string, String, into_bytes};
    use dubhe::dapp_service::UserStorage;
    use dubhe::dapp_system;
    use ville::dapp_key;
    use ville::dapp_key::DappKey;


    const TABLE_NAME: vector<u8> = b"dialogue";
    const OFFCHAIN: bool = true;

    public struct Dialogue has copy, drop, store {
        speaker: address,
        listener: address,
        content: String,
    }

    public fun new(speaker: address, listener: address, content: String): Dialogue {
        Dialogue {
            speaker,
            listener,
            content,
        }
    }

    public fun speaker(self: &Dialogue): address {
        self.speaker
    }

    public fun listener(self: &Dialogue): address {
        self.listener
    }

    public fun content(self: &Dialogue): String {
        self.content
    }

    public fun update_speaker(self: &mut Dialogue, speaker: address) {
        self.speaker = speaker
    }

    public fun update_listener(self: &mut Dialogue, listener: address) {
        self.listener = listener
    }

    public fun update_content(self: &mut Dialogue, content: String) {
        self.content = content
    }

    public(package) fun set(user_storage: &mut UserStorage, speaker: address, listener: address, content: String, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"speaker", b"listener", b"content"];
        let value_tuple = encode(speaker, listener, content);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public(package) fun set_struct(user_storage: &mut UserStorage, dialogue: Dialogue, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"speaker", b"listener", b"content"];
        let value_tuple = encode_struct(dialogue);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun encode(speaker: address, listener: address, content: String): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&speaker));
        value_tuple.push_back(to_bytes(&listener));
        value_tuple.push_back(to_bytes(&into_bytes(content)));
        value_tuple
    }

    public fun encode_struct(dialogue: Dialogue): vector<vector<u8>> {
        encode(dialogue.speaker, dialogue.listener, dialogue.content)
    }
}