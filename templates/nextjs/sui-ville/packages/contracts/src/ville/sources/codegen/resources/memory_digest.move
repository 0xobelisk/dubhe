// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

/* Autogenerated file. Do not edit manually. */

module ville::memory_digest {
    use sui::bcs::{to_bytes};
    use std::ascii::{string, String, into_bytes};
    use dubhe::dapp_service::UserStorage;
    use dubhe::dapp_system;
    use ville::dapp_key;
    use ville::dapp_key::DappKey;


    const TABLE_NAME: vector<u8> = b"memory_digest";
    const OFFCHAIN: bool = false;

    public struct MemoryDigest has copy, drop, store {
        digest: String,
        updated_at: u64,
    }

    public fun new(digest: String, updated_at: u64): MemoryDigest {
        MemoryDigest {
            digest,
            updated_at,
        }
    }

    public fun digest(self: &MemoryDigest): String {
        self.digest
    }

    public fun updated_at(self: &MemoryDigest): u64 {
        self.updated_at
    }

    public fun update_digest(self: &mut MemoryDigest, digest: String) {
        self.digest = digest
    }

    public fun update_updated_at(self: &mut MemoryDigest, updated_at: u64) {
        self.updated_at = updated_at
    }

    public fun has(user_storage: &UserStorage, agent_id: address): bool {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        dapp_system::has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has(user_storage: &UserStorage, agent_id: address) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        dapp_system::ensure_has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has_not(user_storage: &UserStorage, agent_id: address) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        dapp_system::ensure_has_not_record<DappKey>(user_storage, key_tuple)
    }
  

    public(package) fun delete(user_storage: &mut UserStorage, agent_id: address, ctx: &TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        dapp_system::delete_record<DappKey>(dapp_key::new(), user_storage, key_tuple, vector[b"digest", b"updated_at"], ctx);
    }

    public fun get_digest(user_storage: &UserStorage, agent_id: address): String {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        let digest_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"digest");
        let mut digest_bcs = sui::bcs::new(digest_raw);
        let digest = dubhe::bcs::peel_string(&mut digest_bcs);
        digest
    }

    public(package) fun set_digest(user_storage: &mut UserStorage, agent_id: address, digest: String, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        let value = to_bytes(&into_bytes(digest));
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"digest", value, ctx);
    }

    public fun get_updated_at(user_storage: &UserStorage, agent_id: address): u64 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        let updated_at_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"updated_at");
        let mut updated_at_bcs = sui::bcs::new(updated_at_raw);
        let updated_at = sui::bcs::peel_u64(&mut updated_at_bcs);
        updated_at
    }

    public(package) fun set_updated_at(user_storage: &mut UserStorage, agent_id: address, updated_at: u64, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        let value = to_bytes(&updated_at);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"updated_at", value, ctx);
    }

    public fun get(user_storage: &UserStorage, agent_id: address): (String, u64) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        let digest_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"digest");
        let mut digest_bcs = sui::bcs::new(digest_raw);
        let digest = dubhe::bcs::peel_string(&mut digest_bcs);
        let updated_at_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"updated_at");
        let mut updated_at_bcs = sui::bcs::new(updated_at_raw);
        let updated_at = sui::bcs::peel_u64(&mut updated_at_bcs);
        (digest, updated_at)
    }

    public(package) fun set(user_storage: &mut UserStorage, agent_id: address, digest: String, updated_at: u64, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        let field_names = vector[b"digest", b"updated_at"];
        let value_tuple = encode(digest, updated_at);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun get_struct(user_storage: &UserStorage, agent_id: address): MemoryDigest {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        let digest_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"digest");
        let mut digest_bcs = sui::bcs::new(digest_raw);
        let digest = dubhe::bcs::peel_string(&mut digest_bcs);
        let updated_at_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"updated_at");
        let mut updated_at_bcs = sui::bcs::new(updated_at_raw);
        let updated_at = sui::bcs::peel_u64(&mut updated_at_bcs);
        MemoryDigest { digest, updated_at }
    }

    public(package) fun set_struct(user_storage: &mut UserStorage, agent_id: address, memory_digest: MemoryDigest, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&agent_id));
        let field_names = vector[b"digest", b"updated_at"];
        let value_tuple = encode_struct(memory_digest);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun encode(digest: String, updated_at: u64): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&into_bytes(digest)));
        value_tuple.push_back(to_bytes(&updated_at));
        value_tuple
    }

    public fun encode_struct(memory_digest: MemoryDigest): vector<vector<u8>> {
        encode(memory_digest.digest, memory_digest.updated_at)
    }

    public fun decode(data: vector<u8>): MemoryDigest {
        let mut bcs_type = sui::bcs::new(data);
        let digest = string(sui::bcs::peel_vec_u8(&mut bcs_type));
        let updated_at = sui::bcs::peel_u64(&mut bcs_type);
        MemoryDigest {
            digest,
            updated_at,
        }
    }


    // ─── keys: mint (developer provides keys; framework ensures no duplicate) ─
    // Choosing the ID strategy (fresh address, counter, coordinate pack, etc.)
    // is intentionally left to the caller.
    public(package) fun mint(
        user_storage: &mut UserStorage,
        agent_id: address, digest: String, updated_at: u64,
        ctx: &mut TxContext,
    ) {
        ensure_has_not(user_storage, agent_id);
        set(user_storage, agent_id, digest, updated_at, ctx);
    }
}