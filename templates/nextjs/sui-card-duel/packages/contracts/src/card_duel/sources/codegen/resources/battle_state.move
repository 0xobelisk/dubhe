// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

/* Autogenerated file. Do not edit manually. */

module card_duel::battle_state {
    use sui::bcs::{to_bytes};
    use dubhe::dapp_service::UserStorage;
    use dubhe::dapp_service::ScenePermit;
    use dubhe::dapp_system;
    use card_duel::dapp_key;
    use card_duel::dapp_key::DappKey;


    const TABLE_NAME: vector<u8> = b"battle_state";
    const OFFCHAIN: bool = false;

    public struct BattleState has copy, drop, store {
        match_id: address,
        hp: u64,
        shield: u64,
    }

    public fun new(match_id: address, hp: u64, shield: u64): BattleState {
        BattleState {
            match_id,
            hp,
            shield,
        }
    }

    public fun match_id(self: &BattleState): address {
        self.match_id
    }

    public fun hp(self: &BattleState): u64 {
        self.hp
    }

    public fun shield(self: &BattleState): u64 {
        self.shield
    }

    public fun update_match_id(self: &mut BattleState, match_id: address) {
        self.match_id = match_id
    }

    public fun update_hp(self: &mut BattleState, hp: u64) {
        self.hp = hp
    }

    public fun update_shield(self: &mut BattleState, shield: u64) {
        self.shield = shield
    }

    public fun has(user_storage: &UserStorage): bool {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has_not(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_not_record<DappKey>(user_storage, key_tuple)
    }
  

    public(package) fun delete(user_storage: &mut UserStorage, ctx: &TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::delete_record<DappKey>(dapp_key::new(), user_storage, key_tuple, vector[b"match_id", b"hp", b"shield"], ctx);
    }

    public fun get_match_id(user_storage: &UserStorage): address {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let match_id_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"match_id");
        let mut match_id_bcs = sui::bcs::new(match_id_raw);
        let match_id = sui::bcs::peel_address(&mut match_id_bcs);
        match_id
    }

    public(package) fun set_match_id(user_storage: &mut UserStorage, match_id: address, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&match_id);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"match_id", value, ctx);
    }

    public fun get_hp(user_storage: &UserStorage): u64 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let hp_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"hp");
        let mut hp_bcs = sui::bcs::new(hp_raw);
        let hp = sui::bcs::peel_u64(&mut hp_bcs);
        hp
    }

    public(package) fun set_hp(user_storage: &mut UserStorage, hp: u64, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&hp);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"hp", value, ctx);
    }

    public fun get_shield(user_storage: &UserStorage): u64 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let shield_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"shield");
        let mut shield_bcs = sui::bcs::new(shield_raw);
        let shield = sui::bcs::peel_u64(&mut shield_bcs);
        shield
    }

    public(package) fun set_shield(user_storage: &mut UserStorage, shield: u64, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&shield);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"shield", value, ctx);
    }

    public fun get(user_storage: &UserStorage): (address, u64, u64) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let match_id_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"match_id");
        let mut match_id_bcs = sui::bcs::new(match_id_raw);
        let match_id = sui::bcs::peel_address(&mut match_id_bcs);
        let hp_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"hp");
        let mut hp_bcs = sui::bcs::new(hp_raw);
        let hp = sui::bcs::peel_u64(&mut hp_bcs);
        let shield_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"shield");
        let mut shield_bcs = sui::bcs::new(shield_raw);
        let shield = sui::bcs::peel_u64(&mut shield_bcs);
        (match_id, hp, shield)
    }

    public(package) fun set(user_storage: &mut UserStorage, match_id: address, hp: u64, shield: u64, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"match_id", b"hp", b"shield"];
        let value_tuple = encode(match_id, hp, shield);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun get_struct(user_storage: &UserStorage): BattleState {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let match_id_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"match_id");
        let mut match_id_bcs = sui::bcs::new(match_id_raw);
        let match_id = sui::bcs::peel_address(&mut match_id_bcs);
        let hp_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"hp");
        let mut hp_bcs = sui::bcs::new(hp_raw);
        let hp = sui::bcs::peel_u64(&mut hp_bcs);
        let shield_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"shield");
        let mut shield_bcs = sui::bcs::new(shield_raw);
        let shield = sui::bcs::peel_u64(&mut shield_bcs);
        BattleState { match_id, hp, shield }
    }

    public(package) fun set_struct(user_storage: &mut UserStorage, battle_state: BattleState, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"match_id", b"hp", b"shield"];
        let value_tuple = encode_struct(battle_state);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun encode(match_id: address, hp: u64, shield: u64): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&match_id));
        value_tuple.push_back(to_bytes(&hp));
        value_tuple.push_back(to_bytes(&shield));
        value_tuple
    }

    public fun encode_struct(battle_state: BattleState): vector<vector<u8>> {
        encode(battle_state.match_id, battle_state.hp, battle_state.shield)
    }

    public fun decode(data: vector<u8>): BattleState {
        let mut bcs_type = sui::bcs::new(data);
        let match_id = sui::bcs::peel_address(&mut bcs_type);
        let hp = sui::bcs::peel_u64(&mut bcs_type);
        let shield = sui::bcs::peel_u64(&mut bcs_type);
        BattleState {
            match_id,
            hp,
            shield,
        }
    }


    // ─── reactive: cross-user write variants ───────────────────────────
    // Package-level helpers: add pause checks and access control in your system
    // functions before calling these.
    public(package) fun set_reactive<PermType>(
        permit: &ScenePermit<PermType>,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        match_id: address, hp: u64, shield: u64,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"match_id", b"hp", b"shield"];
        let value_tuple = encode(match_id, hp, shield);
        dapp_system::set_record_reactive<DappKey, PermType>(dapp_key::new(), permit, from, target, key_tuple, field_names, value_tuple, ctx);
    }

    public(package) fun set_match_id_reactive<PermType>(
        permit: &ScenePermit<PermType>,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        match_id: address,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = sui::bcs::to_bytes(&match_id);
        dapp_system::set_field_reactive<DappKey, PermType>(dapp_key::new(), permit, from, target, key_tuple, b"match_id", value, ctx);
    }

    public(package) fun set_hp_reactive<PermType>(
        permit: &ScenePermit<PermType>,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        hp: u64,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = sui::bcs::to_bytes(&hp);
        dapp_system::set_field_reactive<DappKey, PermType>(dapp_key::new(), permit, from, target, key_tuple, b"hp", value, ctx);
    }

    public(package) fun set_shield_reactive<PermType>(
        permit: &ScenePermit<PermType>,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        shield: u64,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = sui::bcs::to_bytes(&shield);
        dapp_system::set_field_reactive<DappKey, PermType>(dapp_key::new(), permit, from, target, key_tuple, b"shield", value, ctx);
    }
}