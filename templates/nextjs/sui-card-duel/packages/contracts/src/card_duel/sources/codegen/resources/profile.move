// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

/* Autogenerated file. Do not edit manually. */

module card_duel::profile {
    use sui::bcs::{to_bytes};
    use dubhe::dapp_service::UserStorage;
    use dubhe::dapp_service::ScenePermit;
    use dubhe::dapp_system;
    use card_duel::dapp_key;
    use card_duel::dapp_key::DappKey;


    const TABLE_NAME: vector<u8> = b"profile";
    const OFFCHAIN: bool = false;

    public struct Profile has copy, drop, store {
        wins: u32,
        losses: u32,
        rating: u32,
    }

    public fun new(wins: u32, losses: u32, rating: u32): Profile {
        Profile {
            wins,
            losses,
            rating,
        }
    }

    public fun wins(self: &Profile): u32 {
        self.wins
    }

    public fun losses(self: &Profile): u32 {
        self.losses
    }

    public fun rating(self: &Profile): u32 {
        self.rating
    }

    public fun update_wins(self: &mut Profile, wins: u32) {
        self.wins = wins
    }

    public fun update_losses(self: &mut Profile, losses: u32) {
        self.losses = losses
    }

    public fun update_rating(self: &mut Profile, rating: u32) {
        self.rating = rating
    }

    public fun has(user_storage: &UserStorage): bool {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has_not(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_not_record<DappKey>(user_storage, key_tuple)
    }
  

    public(package) fun delete(user_storage: &mut UserStorage, ctx: &TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::delete_record<DappKey>(dapp_key::new(), user_storage, key_tuple, vector[b"wins", b"losses", b"rating"], ctx);
    }

    public fun get_wins(user_storage: &UserStorage): u32 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let wins_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"wins");
        let mut wins_bcs = sui::bcs::new(wins_raw);
        let wins = sui::bcs::peel_u32(&mut wins_bcs);
        wins
    }

    public(package) fun set_wins(user_storage: &mut UserStorage, wins: u32, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&wins);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"wins", value, ctx);
    }

    public fun get_losses(user_storage: &UserStorage): u32 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let losses_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"losses");
        let mut losses_bcs = sui::bcs::new(losses_raw);
        let losses = sui::bcs::peel_u32(&mut losses_bcs);
        losses
    }

    public(package) fun set_losses(user_storage: &mut UserStorage, losses: u32, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&losses);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"losses", value, ctx);
    }

    public fun get_rating(user_storage: &UserStorage): u32 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let rating_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"rating");
        let mut rating_bcs = sui::bcs::new(rating_raw);
        let rating = sui::bcs::peel_u32(&mut rating_bcs);
        rating
    }

    public(package) fun set_rating(user_storage: &mut UserStorage, rating: u32, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&rating);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"rating", value, ctx);
    }

    public fun get(user_storage: &UserStorage): (u32, u32, u32) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let wins_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"wins");
        let mut wins_bcs = sui::bcs::new(wins_raw);
        let wins = sui::bcs::peel_u32(&mut wins_bcs);
        let losses_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"losses");
        let mut losses_bcs = sui::bcs::new(losses_raw);
        let losses = sui::bcs::peel_u32(&mut losses_bcs);
        let rating_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"rating");
        let mut rating_bcs = sui::bcs::new(rating_raw);
        let rating = sui::bcs::peel_u32(&mut rating_bcs);
        (wins, losses, rating)
    }

    public(package) fun set(user_storage: &mut UserStorage, wins: u32, losses: u32, rating: u32, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"wins", b"losses", b"rating"];
        let value_tuple = encode(wins, losses, rating);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun get_struct(user_storage: &UserStorage): Profile {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let wins_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"wins");
        let mut wins_bcs = sui::bcs::new(wins_raw);
        let wins = sui::bcs::peel_u32(&mut wins_bcs);
        let losses_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"losses");
        let mut losses_bcs = sui::bcs::new(losses_raw);
        let losses = sui::bcs::peel_u32(&mut losses_bcs);
        let rating_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"rating");
        let mut rating_bcs = sui::bcs::new(rating_raw);
        let rating = sui::bcs::peel_u32(&mut rating_bcs);
        Profile { wins, losses, rating }
    }

    public(package) fun set_struct(user_storage: &mut UserStorage, profile: Profile, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"wins", b"losses", b"rating"];
        let value_tuple = encode_struct(profile);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun encode(wins: u32, losses: u32, rating: u32): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&wins));
        value_tuple.push_back(to_bytes(&losses));
        value_tuple.push_back(to_bytes(&rating));
        value_tuple
    }

    public fun encode_struct(profile: Profile): vector<vector<u8>> {
        encode(profile.wins, profile.losses, profile.rating)
    }

    public fun decode(data: vector<u8>): Profile {
        let mut bcs_type = sui::bcs::new(data);
        let wins = sui::bcs::peel_u32(&mut bcs_type);
        let losses = sui::bcs::peel_u32(&mut bcs_type);
        let rating = sui::bcs::peel_u32(&mut bcs_type);
        Profile {
            wins,
            losses,
            rating,
        }
    }


    // ─── reactive: cross-user write variants ───────────────────────────
    // Package-level helpers: add pause checks and access control in your system
    // functions before calling these.
    public(package) fun set_reactive<PermType>(
        permit: &ScenePermit<PermType>,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        wins: u32, losses: u32, rating: u32,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"wins", b"losses", b"rating"];
        let value_tuple = encode(wins, losses, rating);
        dapp_system::set_record_reactive<DappKey, PermType>(dapp_key::new(), permit, from, target, key_tuple, field_names, value_tuple, ctx);
    }

    public(package) fun set_wins_reactive<PermType>(
        permit: &ScenePermit<PermType>,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        wins: u32,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = sui::bcs::to_bytes(&wins);
        dapp_system::set_field_reactive<DappKey, PermType>(dapp_key::new(), permit, from, target, key_tuple, b"wins", value, ctx);
    }

    public(package) fun set_losses_reactive<PermType>(
        permit: &ScenePermit<PermType>,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        losses: u32,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = sui::bcs::to_bytes(&losses);
        dapp_system::set_field_reactive<DappKey, PermType>(dapp_key::new(), permit, from, target, key_tuple, b"losses", value, ctx);
    }

    public(package) fun set_rating_reactive<PermType>(
        permit: &ScenePermit<PermType>,
        from:   &mut UserStorage,
        target: &mut UserStorage,
        rating: u32,
        ctx:    &mut TxContext,
    ) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = sui::bcs::to_bytes(&rating);
        dapp_system::set_field_reactive<DappKey, PermType>(dapp_key::new(), permit, from, target, key_tuple, b"rating", value, ctx);
    }
}