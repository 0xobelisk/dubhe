// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0

/* Autogenerated file. Do not edit manually. */

module card_duel::card {
    use sui::bcs::{to_bytes};
    use dubhe::dapp_service::{UserStorage, DappStorage};
    use dubhe::dapp_system;
    use card_duel::dapp_key;
    use card_duel::dapp_key::DappKey;


    const TABLE_NAME: vector<u8> = b"card";
    const OFFCHAIN: bool = false;

    public struct Card has copy, drop, store {
        kind: u8,
        power: u32,
        rarity: u8,
    }

    public fun new(kind: u8, power: u32, rarity: u8): Card {
        Card {
            kind,
            power,
            rarity,
        }
    }

    public fun kind(self: &Card): u8 {
        self.kind
    }

    public fun power(self: &Card): u32 {
        self.power
    }

    public fun rarity(self: &Card): u8 {
        self.rarity
    }

    public fun update_kind(self: &mut Card, kind: u8) {
        self.kind = kind
    }

    public fun update_power(self: &mut Card, power: u32) {
        self.power = power
    }

    public fun update_rarity(self: &mut Card, rarity: u8) {
        self.rarity = rarity
    }

    public fun has(user_storage: &UserStorage, card_id: address): bool {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        dapp_system::has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has(user_storage: &UserStorage, card_id: address) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        dapp_system::ensure_has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has_not(user_storage: &UserStorage, card_id: address) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        dapp_system::ensure_has_not_record<DappKey>(user_storage, key_tuple)
    }
  

    public(package) fun delete(user_storage: &mut UserStorage, card_id: address, ctx: &TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        dapp_system::delete_record<DappKey>(dapp_key::new(), user_storage, key_tuple, vector[b"kind", b"power", b"rarity"], ctx);
    }

    public fun get_kind(user_storage: &UserStorage, card_id: address): u8 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let kind_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"kind");
        let mut kind_bcs = sui::bcs::new(kind_raw);
        let kind = sui::bcs::peel_u8(&mut kind_bcs);
        kind
    }

    public(package) fun set_kind(user_storage: &mut UserStorage, card_id: address, kind: u8, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let value = to_bytes(&kind);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"kind", value, ctx);
    }

    public fun get_power(user_storage: &UserStorage, card_id: address): u32 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let power_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"power");
        let mut power_bcs = sui::bcs::new(power_raw);
        let power = sui::bcs::peel_u32(&mut power_bcs);
        power
    }

    public(package) fun set_power(user_storage: &mut UserStorage, card_id: address, power: u32, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let value = to_bytes(&power);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"power", value, ctx);
    }

    public fun get_rarity(user_storage: &UserStorage, card_id: address): u8 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let rarity_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"rarity");
        let mut rarity_bcs = sui::bcs::new(rarity_raw);
        let rarity = sui::bcs::peel_u8(&mut rarity_bcs);
        rarity
    }

    public(package) fun set_rarity(user_storage: &mut UserStorage, card_id: address, rarity: u8, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let value = to_bytes(&rarity);
        dapp_system::set_field<DappKey>(dapp_key::new(), user_storage, key_tuple, b"rarity", value, ctx);
    }

    public fun get(user_storage: &UserStorage, card_id: address): (u8, u32, u8) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let kind_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"kind");
        let mut kind_bcs = sui::bcs::new(kind_raw);
        let kind = sui::bcs::peel_u8(&mut kind_bcs);
        let power_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"power");
        let mut power_bcs = sui::bcs::new(power_raw);
        let power = sui::bcs::peel_u32(&mut power_bcs);
        let rarity_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"rarity");
        let mut rarity_bcs = sui::bcs::new(rarity_raw);
        let rarity = sui::bcs::peel_u8(&mut rarity_bcs);
        (kind, power, rarity)
    }

    public(package) fun set(user_storage: &mut UserStorage, card_id: address, kind: u8, power: u32, rarity: u8, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let field_names = vector[b"kind", b"power", b"rarity"];
        let value_tuple = encode(kind, power, rarity);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun get_struct(user_storage: &UserStorage, card_id: address): Card {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let kind_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"kind");
        let mut kind_bcs = sui::bcs::new(kind_raw);
        let kind = sui::bcs::peel_u8(&mut kind_bcs);
        let power_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"power");
        let mut power_bcs = sui::bcs::new(power_raw);
        let power = sui::bcs::peel_u32(&mut power_bcs);
        let rarity_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"rarity");
        let mut rarity_bcs = sui::bcs::new(rarity_raw);
        let rarity = sui::bcs::peel_u8(&mut rarity_bcs);
        Card { kind, power, rarity }
    }

    public(package) fun set_struct(user_storage: &mut UserStorage, card_id: address, card: Card, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        key_tuple.push_back(to_bytes(&card_id));
        let field_names = vector[b"kind", b"power", b"rarity"];
        let value_tuple = encode_struct(card);
        dapp_system::set_record<DappKey>(dapp_key::new(), user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun encode(kind: u8, power: u32, rarity: u8): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&kind));
        value_tuple.push_back(to_bytes(&power));
        value_tuple.push_back(to_bytes(&rarity));
        value_tuple
    }

    public fun encode_struct(card: Card): vector<vector<u8>> {
        encode(card.kind, card.power, card.rarity)
    }

    public fun decode(data: vector<u8>): Card {
        let mut bcs_type = sui::bcs::new(data);
        let kind = sui::bcs::peel_u8(&mut bcs_type);
        let power = sui::bcs::peel_u32(&mut bcs_type);
        let rarity = sui::bcs::peel_u8(&mut bcs_type);
        Card {
            kind,
            power,
            rarity,
        }
    }


    // ─── keys: mint (developer provides keys; framework ensures no duplicate) ─
    // Choosing the ID strategy (fresh address, counter, coordinate pack, etc.)
    // is intentionally left to the caller.
    public(package) fun mint(
        user_storage: &mut UserStorage,
        card_id: address, kind: u8, power: u32, rarity: u8,
        ctx: &mut TxContext,
    ) {
        ensure_has_not(user_storage, card_id);
        set(user_storage, card_id, kind, power, rarity, ctx);
    }

    // ─── listable: market protocol (keyed) ──────────────────────────────
    // Package-level helpers: call these from your system functions.
    // Add pause checks, access control, and custom logic there.
    public(package) fun list<CoinType>(
        user_storage: &mut UserStorage,
        card_id:   address,
        price:        u64,
        listed_until: std::option::Option<u64>,
        ctx:          &mut TxContext,
    ) {
        let mut record_key = vector::empty();
        record_key.push_back(TABLE_NAME);
        record_key.push_back(sui::bcs::to_bytes(&card_id));
        dubhe::dapp_system::take_record<DappKey, CoinType>(
            dapp_key::new(),
            user_storage,
            b"card",
            record_key,
            vector[b"kind", b"power", b"rarity"],
            price,
            listed_until,
            ctx,
        );
    }

    public(package) fun buy<CoinType>(
        dh:            &dubhe::dapp_service::DappHub,
        dapp_storage:  &mut DappStorage,
        listing:       dubhe::dapp_service::Listing<CoinType>,
        user_storage:  &mut UserStorage,
        payment:       sui::coin::Coin<CoinType>,
        ctx:           &mut TxContext,
    ): sui::coin::Coin<CoinType> {
        dubhe::dapp_system::buy_record<DappKey, CoinType>(
            dapp_key::new(), dh, dapp_storage, listing, user_storage, payment, ctx
        )
    }

    public(package) fun cancel_listing<CoinType>(
        listing:      dubhe::dapp_service::Listing<CoinType>,
        user_storage: &mut UserStorage,
        ctx:          &TxContext,
    ) {
        dubhe::dapp_system::restore_record<DappKey, CoinType>(
            dapp_key::new(), listing, user_storage, ctx
        );
    }

    public(package) fun expire_listing<CoinType>(
        listing:      dubhe::dapp_service::Listing<CoinType>,
        user_storage: &mut UserStorage,
        ctx:          &TxContext,
    ) {
        dubhe::dapp_system::expire_listing<DappKey, CoinType>(
            dapp_key::new(), listing, user_storage, ctx
        );
    }
}