// Copyright (c) Obelisk Labs, Inc.
// SPDX-License-Identifier: Apache-2.0
#[allow(unused_use)]

/* Autogenerated file. Do not edit manually. */

module counter::counter2 {
    use sui::bcs::{to_bytes};
    use std::ascii::{string, String, into_bytes};
    use dubhe::table_id;
    use dubhe::dapp_service::{Self, UserStorage, DappHub};
    use dubhe::dapp_system;
    use counter::dapp_key;
    use counter::dapp_key::DappKey;


    const TABLE_NAME: vector<u8> = b"counter2";
    const OFFCHAIN: bool = false;

    public struct Counter2 has copy, drop, store {
        value: u32,
        data: u64,
        key: String,
    }

    public fun new(value: u32, data: u64, key: String): Counter2 {
        Counter2 {
            value,
            data,
            key,
        }
    }

    public fun value(self: &Counter2): u32 {
        self.value
    }

    public fun data(self: &Counter2): u64 {
        self.data
    }

    public fun key(self: &Counter2): String {
        self.key
    }

    public fun update_value(self: &mut Counter2, value: u32) {
        self.value = value
    }

    public fun update_data(self: &mut Counter2, data: u64) {
        self.data = data
    }

    public fun update_key(self: &mut Counter2, key: String) {
        self.key = key
    }

    public fun has(user_storage: &UserStorage): bool {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_record<DappKey>(user_storage, key_tuple)
    }

    public fun ensure_has_not(user_storage: &UserStorage) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::ensure_has_not_record<DappKey>(user_storage, key_tuple)
    }
  

    public(package) fun delete(user_storage: &mut UserStorage, ctx: &TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        dapp_system::delete_record<DappKey>(dapp_key::new(), user_storage, key_tuple, vector[b"value", b"data", b"key"], ctx);
    }

    public fun get_value(user_storage: &UserStorage): u32 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"value");
        let mut value_bcs = sui::bcs::new(value_raw);
        let value = sui::bcs::peel_u32(&mut value_bcs);
        value
    }

    public(package) fun set_value(dapp_hub: &DappHub, user_storage: &mut UserStorage, value: u32, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&value);
        dapp_system::set_field<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, b"value", value, ctx);
    }

    public fun get_data(user_storage: &UserStorage): u64 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let data_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"data");
        let mut data_bcs = sui::bcs::new(data_raw);
        let data = sui::bcs::peel_u64(&mut data_bcs);
        data
    }

    public(package) fun set_data(dapp_hub: &DappHub, user_storage: &mut UserStorage, data: u64, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&data);
        dapp_system::set_field<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, b"data", value, ctx);
    }

    public fun get_key(user_storage: &UserStorage): String {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let key_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"key");
        let mut key_bcs = sui::bcs::new(key_raw);
        let key = dubhe::bcs::peel_string(&mut key_bcs);
        key
    }

    public(package) fun set_key(dapp_hub: &DappHub, user_storage: &mut UserStorage, key: String, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value = to_bytes(&into_bytes(key));
        dapp_system::set_field<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, b"key", value, ctx);
    }

    public fun get(user_storage: &UserStorage): (u32, u64, String) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"value");
        let mut value_bcs = sui::bcs::new(value_raw);
        let value = sui::bcs::peel_u32(&mut value_bcs);
        let data_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"data");
        let mut data_bcs = sui::bcs::new(data_raw);
        let data = sui::bcs::peel_u64(&mut data_bcs);
        let key_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"key");
        let mut key_bcs = sui::bcs::new(key_raw);
        let key = dubhe::bcs::peel_string(&mut key_bcs);
        (value, data, key)
    }

    public(package) fun set(dapp_hub: &DappHub, user_storage: &mut UserStorage, value: u32, data: u64, key: String, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"value", b"data", b"key"];
        let value_tuple = encode(value, data, key);
        dapp_system::set_record<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun get_struct(user_storage: &UserStorage): Counter2 {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let value_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"value");
        let mut value_bcs = sui::bcs::new(value_raw);
        let value = sui::bcs::peel_u32(&mut value_bcs);
        let data_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"data");
        let mut data_bcs = sui::bcs::new(data_raw);
        let data = sui::bcs::peel_u64(&mut data_bcs);
        let key_raw = dapp_system::get_field<DappKey>(user_storage, key_tuple, b"key");
        let mut key_bcs = sui::bcs::new(key_raw);
        let key = dubhe::bcs::peel_string(&mut key_bcs);
        Counter2 { value, data, key }
    }

    public(package) fun set_struct(dapp_hub: &DappHub, user_storage: &mut UserStorage, counter2: Counter2, ctx: &mut TxContext) {
        let mut key_tuple = vector::empty();
        key_tuple.push_back(TABLE_NAME);
        let field_names = vector[b"value", b"data", b"key"];
        let value_tuple = encode_struct(counter2);
        dapp_system::set_record<DappKey>(dapp_key::new(), dapp_hub, user_storage, key_tuple, field_names, value_tuple, OFFCHAIN, ctx);
    }

    public fun encode(value: u32, data: u64, key: String): vector<vector<u8>> {
        let mut value_tuple = vector::empty();
        value_tuple.push_back(to_bytes(&value));
        value_tuple.push_back(to_bytes(&data));
        value_tuple.push_back(to_bytes(&into_bytes(key)));
        value_tuple
    }

    public fun encode_struct(counter2: Counter2): vector<vector<u8>> {
        encode(counter2.value, counter2.data, counter2.key)
    }

    public fun decode(data: vector<u8>): Counter2 {
        let mut bcs_type = sui::bcs::new(data);
        let value = sui::bcs::peel_u32(&mut bcs_type);
        let data = sui::bcs::peel_u64(&mut bcs_type);
        let key = string(sui::bcs::peel_vec_u8(&mut bcs_type));
        Counter2 {
            value,
            data,
            key,
        }
    }
}